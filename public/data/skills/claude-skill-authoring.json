{
  "source": "shyft.ai",
  "url": "https://shyft.ai/skills/claude-skill-authoring",
  "slug": "claude-skill-authoring",
  "name": "Claude Skill Authoring",
  "headline": "Create and validate Claude Code skills effectively",
  "short_description": "Claude Skill Authoring is a Python-based tool that guides Claude in creating and validating Claude Code Skills. It follows Anthropic's official best practices, making it ideal for operations teams looking to automate workflows. The skill integrates with Claude's infrastructure, enabling rapid development and deployment of custom skills.",
  "tier": "Silver",
  "category": "Marketing",
  "difficulty": "Intermediate",
  "install_type": "Git Clone",
  "estimated_time_saving": "60 min",
  "author": "IndianOldTurtledove",
  "install_command": "git clone https://github.com/IndianOldTurtledove/claude-skill-authoring.git",
  "source_repo_url": "https://github.com/IndianOldTurtledove/claude-skill-authoring",
  "works_with": [
    "Claude"
  ],
  "tags": [],
  "sections": {
    "overview": "Codex OSS Maintainer Toolkit is a repository structure and automation framework designed for open-source maintainers managing Codex-based workflows. It provides templates and scripts for creating AGENTS.md files, developing reusable skills, and automating common maintenance tasks like issue triage, debugging, and validation. The toolkit includes pre-built automation scripts (issue recommendation, change investigation, file verification), maintainer agent prompts for code review and error resolution, and a dev docs system for cross-session context retention. Installation supports both global Codex skill registration and per-project scaffolding, making it ideal for teams standardizing their AI-assisted maintenance processes.\nSetting up AGENTS.md and skill architecture in new open-source projects\nAutomating issue triage and PR review workflows with Codex agents\nCreating and validating reusable Claude skills for maintenance tasks\nStandardizing debugging and change investigation processes across repositories",
    "setup": "No install command available. Check the GitHub repository for manual installation instructions.\n`git clone https://github.com/IndianOldTurtledove/claude-skill-authoring`",
    "usage": "```\nUse the claude-skill-authoring tool to create a new Claude Code Skill that [DESCRIBE THE SKILL'S PURPOSE, e.g., 'automates weekly data backup for a company's SQL databases']. Follow Anthropic's best practices for skill structure, validation, and error handling. Include unit tests and a README.md file. The skill should integrate with [SPECIFIC TOOLS/APIs, e.g., 'AWS S3 for storage and Slack for notifications'] and handle [EDGE CASES, e.g., 'failed backups or network timeouts'].\n```\nCopy\n\n```\n```python\n# File: weekly_sql_backup.py\n# Description: Automates weekly backups of SQL databases to AWS S3 with Slack notifications.\n# Author: AI Assistant\n# Version: 1.0.0\n\nimport os\nimport subprocess\nimport boto3\nfrom datetime import datetime\nimport requests\nimport logging\n\n# Configure logging\nlogging.basicConfig(level=logging.INFO)\nlogger = logging.getLogger(__name__)\n\nclass WeeklySQLBackup:\n    def __init__(self, db_host, db_user, db_pass, s3_bucket, slack_webhook):\n        self.db_host = db_host\n        self.db_user = db_user\n        self.db_pass = db_pass\n        self.s3_bucket = s3_bucket\n        self.slack_webhook = slack_webhook\n        self.s3_client = boto3.client('s3')\n\n    def backup_database(self, db_name):\n        \"\"\"Create a backup of the specified database and upload to S3.\"\"\"\n        timestamp = datetime.now().strftime('%Y%m%d_%H%M%S')\n        backup_file = f\"/tmp/{db_name}_{timestamp}.sql\"\n        s3_key = f\"backups/{db_name}/{timestamp}.sql\"\n\n        try:\n            # Dump database\n            cmd = f\"mysqldump -h {self.db_host} -u {self.db_user} -p{self.db_pass} {db_name} > {backup_file}\"\n            subprocess.run(cmd, shell=True, check=True, capture_output=True)\n\n            # Upload to S3\n            self.s3_client.upload_file(backup_file, self.s3_bucket, s3_key)\n            logger.info(f\"Backup of {db_name} uploaded to S3 as {s3_key}\")\n\n            # Clean up\n            os.remove(backup_file)\n            return True\n        except subprocess.CalledProcessError as e:\n            logger.error(f\"Database backup failed: {e.stderr.decode()}\")\n            self._notify_slack(f\"❌ Backup failed for {db_name}: {e.stderr.decode()}\")\n            return False\n        except Exception as e:\n            logger.error(f\"Unexpected error during backup: {str(e)}\")\n            self._notify_slack(f\"❌ Unexpected error for {db_name}: {str(e)}\")\n            return False\n\n    def _notify_slack(self, message):\n        \"\"\"Send a notification to Slack via webhook.\"\"\"\n        payload = {\"text\": message}\n        try:\n            requests.post(self.slack_webhook, json=payload, timeout=10)\n        except Exception as e:\n            logger.error(f\"Failed to send Slack notification: {str(e)}\")\n\n# Unit tests\nimport unittest\nfrom unittest.mock import patch, MagicMock\n\nclass TestWeeklySQLBackup(unittest.TestCase):\n    def setUp(self):\n        self.skill = WeeklySQLBackup(\n            db_host=\"localhost\",\n            db_user=\"admin\",\n            db_pass=\"password\",\n            s3_bucket=\"my-backups\",\n            slack_webhook=\"https://hooks.slack.com/services/XXX/YYY/ZZZ\"\n        )\n\n    @patch('subprocess.run')\n    @patch('boto3.client')\n    def test_backup_success(self, mock_boto, mock_subprocess):\n        mock_subprocess.return_value = MagicMock(returncode=0)\n        mock_boto.return_value.upload_file.return_value = None\n        result = self.skill.backup_database(\"test_db\")\n        self.assertTrue(result)\n\n    @patch('subprocess.run')\n    def test_backup_failure(self, mock_subprocess):\n        mock_subprocess.side_effect = subprocess.CalledProcessError(1, \"mysqldump\")\n        result = self.skill.backup_database(\"test_db\")\n        self.assertFalse(result)\n\nif __name__ == \"__main__\":\n    # Example usage\n    skill = WeeklySQLBackup(\n        db_host=\"prod-db.example.com\",\n        db_user=\"backup_user\",\n        db_pass=\"secure_password123\",\n        s3_bucket=\"company-backups\",\n        slack_webhook=\"https://hooks.slack.com/services/ABC123/DEF456/GHI789\"\n    )\n\n    # Backup all databases\n    databases = [\"sales_db\", \"inventory_db\", \"user_db\"]\n    for db in databases:\n        if not skill.backup_database(db):\n            logger.error(f\"Skipping {db} due to backup failure\")\n```\n\n```markdown\n# README.md",
    "requirements": [
      "Claude Code or compatible AI agent",
      "Works with: Claude"
    ],
    "related_skills": [
      "claude-skill-authoring#main-content",
      "claude-skill-authoring#overview",
      "claude-skill-authoring#setup",
      "claude-skill-authoring#examples",
      "claude-skill-authoring#tools",
      "claude-skill-authoring#mcp",
      "claude-skill-authoring#related",
      "find-skills",
      "vercel-composition-patterns",
      "browser-use"
    ],
    "overview_vi": "Codex OSS Maintainer Toolkit là một cấu trúc repository và framework tự động hóa được thiết kế dành cho các maintainer dự án mã nguồn mở quản lý các workflow dựa trên Codex. Bộ công cụ cung cấp các template và script để tạo file AGENTS.md, phát triển các skill có thể tái sử dụng, và tự động hóa các tác vụ bảo trì phổ biến như phân loại (triage) issue, debug, và xác thực. Bộ công cụ bao gồm các script tự động hóa được xây dựng sẵn (gợi ý issue, điều tra thay đổi, xác minh file), các prompt cho agent của maintainer để review code và sửa lỗi, cùng hệ thống tài liệu phát triển (dev docs) giúp duy trì ngữ cảnh giữa các phiên làm việc (cross-session). Quá trình cài đặt hỗ trợ cả việc đăng ký Codex skill toàn cục (global) lẫn dựng khung (scaffolding) theo từng dự án, là giải pháp lý tưởng cho các đội ngũ muốn chuẩn hóa quy trình bảo trì được hỗ trợ bởi AI.\nThiết lập AGENTS.md và kiến trúc skill trong các dự án mã nguồn mở mới\nTự động hóa workflow phân loại issue và review PR bằng Codex agent\nTạo và xác thực các Claude skill có thể tái sử dụng cho các tác vụ bảo trì\nChuẩn hóa quy trình debug và điều tra thay đổi trên các repository",
    "setup_vi": "Không có sẵn lệnh cài đặt trực tiếp. Vui lòng kiểm tra repository trên GitHub để xem hướng dẫn cài đặt thủ công.\n`git clone https://github.com/IndianOldTurtledove/claude-skill-authoring`",
    "usage_vi": "```\nSử dụng công cụ claude-skill-authoring để tạo một Claude Code Skill mới nhằm [MÔ TẢ MỤC ĐÍCH CỦA SKILL, ví dụ: 'tự động sao lưu dữ liệu hàng tuần cho các cơ sở dữ liệu SQL của công ty']. Tuân thủ các best practice của Anthropic về cấu trúc skill, xác thực và xử lý lỗi. Hãy bao gồm cả unit test và file README.md. Skill này cần tích hợp với [CÔNG CỤ/API CỤ THỂ, ví dụ: 'AWS S3 để lưu trữ và Slack để nhận thông báo'] và xử lý [CÁC TRƯỜNG HỢP NGOẠI LỆ, ví dụ: 'sao lưu thất bại hoặc lỗi timeout mạng'].\n```\nCopy\n\n```\n```python\n# File: weekly_sql_backup.py\n# Description: Tự động hóa việc sao lưu hàng tuần các cơ sở dữ liệu SQL lên AWS S3 và gửi thông báo qua Slack.\n# Author: AI Assistant\n# Version: 1.0.0\n\nimport os\nimport subprocess\nimport boto3\nfrom datetime import datetime\nimport requests\nimport logging\n\n# Cấu hình logging\nlogging.basicConfig(level=logging.INFO)\nlogger = logging.getLogger(__name__)\n\nclass WeeklySQLBackup:\n    def __init__(self, db_host, db_user, db_pass, s3_bucket, slack_webhook):\n        self.db_host = db_host\n        self.db_user = db_user\n        self.db_pass = db_pass\n        self.s3_bucket = s3_bucket\n        self.slack_webhook = slack_webhook\n        self.s3_client = boto3.client('s3')\n\n    def backup_database(self, db_name):\n        \"\"\"Tạo bản sao lưu cho cơ sở dữ liệu được chỉ định và tải lên S3.\"\"\"\n        timestamp = datetime.now().strftime('%Y%m%d_%H%M%S')\n        backup_file = f\"/tmp/{db_name}_{timestamp}.sql\"\n        s3_key = f\"backups/{db_name}/{timestamp}.sql\"\n\n        try:\n            # Xuất bản sao lưu database\n            cmd = f\"mysqldump -h {self.db_host} -u {self.db_user} -p{self.db_pass} {db_name} > {backup_file}\"\n            subprocess.run(cmd, shell=True, check=True, capture_output=True)\n\n            # Tải lên S3\n            self.s3_client.upload_file(backup_file, self.s3_bucket, s3_key)\n            logger.info(f\"Backup of {db_name} uploaded to S3 as {s3_key}\")\n\n            # Dọn dẹp file tạm\n            os.remove(backup_file)\n            return True\n        except subprocess.CalledProcessError as e:\n            logger.error(f\"Database backup failed: {e.stderr.decode()}\")\n            self._notify_slack(f\"❌ Backup failed for {db_name}: {e.stderr.decode()}\")\n            return False\n        except Exception as e:\n            logger.error(f\"Unexpected error during backup: {str(e)}\")\n            self._notify_slack(f\"❌ Unexpected error for {db_name}: {str(e)}\")\n            return False\n\n    def _notify_slack(self, message):\n        \"\"\"Gửi thông báo đến Slack qua webhook.\"\"\"\n        payload = {\"text\": message}\n        try:\n            requests.post(self.slack_webhook, json=payload, timeout=10)\n        except Exception as e:\n            logger.error(f\"Failed to send Slack notification: {str(e)}\")\n\n# Unit test\nimport unittest\nfrom unittest.mock import patch, MagicMock\n\nclass TestWeeklySQLBackup(unittest.TestCase):\n    def setUp(self):\n        self.skill = WeeklySQLBackup(\n            db_host=\"localhost\",\n            db_user=\"admin\",\n            db_pass=\"password\",\n            s3_bucket=\"my-backups\",\n            slack_webhook=\"https://hooks.slack.com/services/XXX/YYY/ZZZ\"\n        )\n\n    @patch('subprocess.run')\n    @patch('boto3.client')\n    def test_backup_success(self, mock_boto, mock_subprocess):\n        mock_subprocess.return_value = MagicMock(returncode=0)\n        mock_boto.return_value.upload_file.return_value = None\n        result = self.skill.backup_database(\"test_db\")\n        self.assertTrue(result)\n\n    @patch('subprocess.run')\n    def test_backup_failure(self, mock_subprocess):\n        mock_subprocess.side_effect = subprocess.CalledProcessError(1, \"mysqldump\")\n        result = self.skill.backup_database(\"test_db\")\n        self.assertFalse(result)\n\nif __name__ == \"__main__\":\n    # Ví dụ sử dụng\n    skill = WeeklySQLBackup(\n        db_host=\"prod-db.example.com\",\n        db_user=\"backup_user\",\n        db_pass=\"secure_password123\",\n        s3_bucket=\"company-backups\",\n        slack_webhook=\"https://hooks.slack.com/services/ABC123/DEF456/GHI789\"\n    )\n\n    # Sao lưu tất cả database\n    databases = [\"sales_db\", \"inventory_db\", \"user_db\"]\n    for db in databases:\n        if not skill.backup_database(db):\n            logger.error(f\"Skipping {db} due to backup failure\")\n```\n\n```markdown\n# README.md\""
  },
  "crawl_meta": {
    "crawled_at": "2026-06-24T09:00:59.369316Z",
    "http_status": 200,
    "content_hash": "d7fd40ed7fe6679d60d2fd1513024184d9dbf71131c4816ce8d216ff5e3b2fa2",
    "raw_markdown_path": "D:\\Dev\\2.reference_pj\\.skill-ref\\crawl\\.shifty-crawl\\output\\shyft-skills\\raw\\claude-skill-authoring.md",
    "raw_html_path": "D:\\Dev\\2.reference_pj\\.skill-ref\\crawl\\.shifty-crawl\\output\\shyft-skills\\raw\\claude-skill-authoring.html",
    "schema_version": "1.0"
  },
  "headline_vi": "Xây dựng và kiểm thử hiệu quả các skill cho Claude Code",
  "short_description_vi": "Claude Skill Authoring là công cụ phát triển bằng Python giúp hướng dẫn Claude tạo và kiểm thử các Claude Code skill. Công cụ này tuân thủ các best practice chính thức từ Anthropic, lý tưởng cho các đội ngũ vận hành (operations) muốn tự động hóa workflow. Skill này tích hợp chặt chẽ với hạ tầng của Claude, giúp rút ngắn thời gian phát triển và triển khai các skill tùy chỉnh."
}