{
  "source": "shyft.ai",
  "url": "https://shyft.ai/skills/gentleman-skills",
  "slug": "gentleman-skills",
  "name": "Gentleman-Skills",
  "headline": "Gentleman-Skills - AI Agent Skills for Code",
  "short_description": "Gentleman-Skills provides community-curated AI agent skills for Claude Code, OpenCode, and other AI assistants. It enables AI to write code following specific frameworks, libraries, and patterns. Developers and operations teams benefit from standardized, reusable code patterns. It connects to AI assistants and integrates with development workflows.",
  "tier": "Silver",
  "category": "Marketing",
  "difficulty": "Intermediate",
  "install_type": "Git Clone",
  "estimated_time_saving": "120 min",
  "author": "Gentleman-Programming",
  "install_command": "git clone https://github.com/Gentleman-Programming/Gentleman-Skills.git",
  "source_repo_url": "https://github.com/Gentleman-Programming/Gentleman-Skills",
  "works_with": [
    "Claude"
  ],
  "tags": [],
  "sections": {
    "overview": "Gentleman-Skills is a collection of specialized instruction sets designed to teach AI assistants like Claude Code, Cursor, and Gemini CLI how to write code following best practices. The repository provides both personally curated skills and community-contributed skills covering frontend frameworks (React, Angular, Next.js), backend tools (Django, Spring Boot), testing (Playwright, pytest), and workflow automation (Jira, GitHub). Skills work by providing on-demand context and patterns that guide AI code generation, helping developers maintain consistent architecture, file naming conventions, and coding standards across projects.\nStandardizing Angular component patterns with signals and zoneless architecture\nGenerating React 19 code with React Compiler optimization\nBuilding Next.js 15 applications using App Router patterns\nCreating E2E tests with Playwright automation",
    "setup": "No install command available. Check the GitHub repository for manual installation instructions.\n`git clone https://github.com/Gentleman-Programming/Gentleman-Skills`",
    "usage": "```\nUse Gentleman-Skills to generate a Python class following the [FRAMEWORK/LIBRARY] pattern for [PURPOSE]. Ensure the class includes [REQUIRED_METHODS] and adheres to [CODING_STANDARDS]. Include docstrings and type hints. Example: 'Generate a FastAPI route handler for a user authentication service following RESTful conventions. Include endpoints for login, logout, and token refresh. Add proper error handling and logging.'\n```\nCopy\n\n```\n```python\nfrom fastapi import FastAPI, HTTPException, status, Depends\nfrom fastapi.security import OAuth2PasswordBearer, OAuth2PasswordRequestForm\nfrom pydantic import BaseModel\nfrom typing import Optional\nimport logging\nimport secrets\n\napp = FastAPI()\nlogger = logging.getLogger(__name__)\n\n# Models\nclass Token(BaseModel):\n    access_token: str\n    token_type: str\n\nclass User(BaseModel):\n    username: str\n    email: Optional[str] = None\n\n# Mock database\nfake_users_db = {\n    \"admin\": {\n        \"username\": \"admin\",\n        \"email\": \"admin@example.com\",\n        \"hashed_password\": \"fakehashedsecret\",\n        \"disabled\": False,\n    }\n}\n\n# OAuth2 scheme\noauth2_scheme = OAuth2PasswordBearer(tokenUrl=\"token\")\n\n# Utility functions\ndef verify_password(plain_password: str, hashed_password: str):\n    return secrets.compare_digest(plain_password, hashed_password)\n\ndef get_user(db, username: str):\n    if username in db:\n        user_dict = db[username]\n        return User(**user_dict)\n    return None\n\ndef authenticate_user(fake_db, username: str, password: str):\n    user = get_user(fake_db, username)\n    if not user or not verify_password(password, fake_db[username][\"hashed_password\"]):\n        return False\n    return user\n\ndef create_access_token(data: dict, expires_delta: Optional[int] = None):\n    to_encode = data.copy()\n    return \"generated.jwt.token\"\n\n# Route handlers\n@app.post(\"/token\", response_model=Token)\nasync def login(form_data: OAuth2PasswordRequestForm = Depends()):\n    user = authenticate_user(fake_users_db, form_data.username, form_data.password)\n    if not user:\n        logger.warning(f\"Failed login attempt for user: {form_data.username}\")\n        raise HTTPException(\n            status_code=status.HTTP_401_UNAUTHORIZED,\n            detail=\"Incorrect username or password\",\n            headers={\"WWW-Authenticate\": \"Bearer\"},\n        )\n    access_token_expires = 3600  # 1 hour\n    access_token = create_access_token(\n        data={\"sub\": user.username}, expires_delta=access_token_expires\n    )\n    logger.info(f\"User {user.username} logged in successfully\")\n    return {\"access_token\": access_token, \"token_type\": \"bearer\"}\n\n@app.get(\"/users/me\", response_model=User)\nasync def read_users_me(token: str = Depends(oauth2_scheme)):\n    current_user = get_user(fake_users_db, token)\n    if current_user is None:\n        raise HTTPException(status_code=404, detail=\"User not found\")\n    return current_user\n```\n\nThis FastAPI authentication service follows RESTful conventions with proper error handling, logging, and type safety. The class structure adheres to FastAPI's recommended patterns, including dependency injection for OAuth2 and Pydantic models for request/response validation. The mock database simulates a production-ready implementation that can be replaced with a real database connection.\n```",
    "requirements": [
      "Claude Code or compatible AI agent",
      "Works with: Claude"
    ],
    "related_skills": [
      "gentleman-skills#main-content",
      "gentleman-skills#overview",
      "gentleman-skills#setup",
      "gentleman-skills#examples",
      "gentleman-skills#tools",
      "gentleman-skills#mcp",
      "gentleman-skills#related",
      "find-skills",
      "vercel-composition-patterns",
      "browser-use"
    ],
    "overview_vi": "Gentleman-Skills là một tập hợp các bộ hướng dẫn chuyên biệt được thiết kế để chỉ dẫn các trợ lý AI như Claude Code, Cursor và Gemini CLI cách viết code tuân theo các best practice. Repository này cung cấp cả các skill do tác giả tự tuyển chọn lẫn các skill do cộng đồng đóng góp, bao gồm các frontend framework (React, Angular, Next.js), backend tool (Django, Spring Boot), kiểm thử (Playwright, pytest) và tự động hóa quy trình làm việc (Jira, GitHub). Các skill hoạt động bằng cách cung cấp ngữ cảnh (context) và pattern theo yêu cầu để định hướng quá trình sinh code của AI, giúp các lập trình viên duy trì cấu trúc kiến trúc (architecture) đồng nhất, quy ước đặt tên file và các tiêu chuẩn coding (coding standards) trên toàn bộ dự án.\nChuẩn hóa các Angular component pattern với signals và kiến trúc zoneless\nTự động sinh code React 19 tối ưu hóa bằng React Compiler\nXây dựng các ứng dụng Next.js 15 sử dụng các App Router pattern\nTạo các bài kiểm thử E2E tự động với Playwright",
    "setup_vi": "Không có sẵn lệnh cài đặt. Vui lòng kiểm tra GitHub repository để xem hướng dẫn cài đặt thủ công.\n`git clone https://github.com/Gentleman-Programming/Gentleman-Skills`",
    "usage_vi": "```\nSử dụng Gentleman-Skills để sinh một class Python theo pattern [FRAMEWORK/LIBRARY] cho [PURPOSE]. Đảm bảo class bao gồm [REQUIRED_METHODS] và tuân thủ [CODING_STANDARDS]. Đi kèm docstring và gợi ý kiểu (type hint). Ví dụ: 'Tạo một route handler bằng FastAPI cho dịch vụ xác thực người dùng tuân theo quy ước RESTful. Bao gồm các endpoint đăng nhập (login), đăng xuất (logout) và làm mới token (token refresh). Thêm xử lý lỗi và ghi log (logging) phù hợp.'\n```\nCopy\n\n```\n```python\nfrom fastapi import FastAPI, HTTPException, status, Depends\nfrom fastapi.security import OAuth2PasswordBearer, OAuth2PasswordRequestForm\nfrom pydantic import BaseModel\nfrom typing import Optional\nimport logging\nimport secrets\n\napp = FastAPI()\nlogger = logging.getLogger(__name__)\n\n# Model\nclass Token(BaseModel):\n    access_token: str\n    token_type: str\n\nclass User(BaseModel):\n    username: str\n    email: Optional[str] = None\n\n# Database giả lập (Mock)\nfake_users_db = {\n    \"admin\": {\n        \"username\": \"admin\",\n        \"email\": \"admin@example.com\",\n        \"hashed_password\": \"fakehashedsecret\",\n        \"disabled\": False,\n    }\n}\n\n# Cơ chế OAuth2\noauth2_scheme = OAuth2PasswordBearer(tokenUrl=\"token\")\n\n# Các hàm tiện ích\ndef verify_password(plain_password: str, hashed_password: str):\n    return secrets.compare_digest(plain_password, hashed_password)\n\ndef get_user(db, username: str):\n    if username in db:\n        user_dict = db[username]\n        return User(**user_dict)\n    return None\n\ndef authenticate_user(fake_db, username: str, password: str):\n    user = get_user(fake_db, username)\n    if not user or not verify_password(password, fake_db[username][\"hashed_password\"]):\n        return False\n    return user\n\ndef create_access_token(data: dict, expires_delta: Optional[int] = None):\n    to_encode = data.copy()\n    return \"generated.jwt.token\"\n\n# Route handler\n@app.post(\"/token\", response_model=Token)\nasync def login(form_data: OAuth2PasswordRequestForm = Depends()):\n    user = authenticate_user(fake_users_db, form_data.username, form_data.password)\n    if not user:\n        logger.warning(f\"Failed login attempt for user: {form_data.username}\")\n        raise HTTPException(\n            status_code=status.HTTP_401_UNAUTHORIZED,\n            detail=\"Incorrect username or password\",\n            headers={\"WWW-Authenticate\": \"Bearer\"},\n        )\n    access_token_expires = 3600  # 1 hour\n    access_token = create_access_token(\n        data={\"sub\": user.username}, expires_delta=access_token_expires\n    )\n    logger.info(f\"User {user.username} logged in successfully\")\n    return {\"access_token\": access_token, \"token_type\": \"bearer\"}\n\n@app.get(\"/users/me\", response_model=User)\nasync def read_users_me(token: str = Depends(oauth2_scheme)):\n    current_user = get_user(fake_users_db, token)\n    if current_user is None:\n        raise HTTPException(status_code=404, detail=\"User not found\")\n    return current_user\n```\n\nDịch vụ xác thực FastAPI này tuân thủ các quy ước RESTful với đầy đủ cơ chế xử lý lỗi, logging và an toàn kiểu dữ liệu (type safety). Cấu trúc class tuân theo các pattern được khuyến nghị của FastAPI, bao gồm dependency injection cho OAuth2 và các model Pydantic để validate request/response. Database giả lập mô phỏng một triển khai thực tế có thể sẵn sàng đưa vào sản xuất (production-ready) và dễ dàng thay thế bằng một kết nối database thật."
  },
  "crawl_meta": {
    "crawled_at": "2026-06-24T08:47:20.730941Z",
    "http_status": 200,
    "content_hash": "60d2bc108790a9d695ef84ed616a9cef46229f3ffef6ff45d3cc68fe8b9f1ad3",
    "raw_markdown_path": "D:\\Dev\\2.reference_pj\\.skill-ref\\crawl\\.shifty-crawl\\output\\shyft-skills\\raw\\gentleman-skills.md",
    "raw_html_path": "D:\\Dev\\2.reference_pj\\.skill-ref\\crawl\\.shifty-crawl\\output\\shyft-skills\\raw\\gentleman-skills.html",
    "schema_version": "1.0"
  },
  "headline_vi": "Gentleman-Skills - AI Agent Skills dành cho Code",
  "short_description_vi": "Gentleman-Skills cung cấp các AI agent skills được tuyển chọn từ cộng đồng cho Claude Code, OpenCode và các trợ lý AI khác. Công cụ này giúp AI viết code tuân theo các framework, thư viện và pattern cụ thể. Các nhà phát triển và đội ngũ vận hành sẽ được hưởng lợi từ các code pattern chuẩn hóa, có thể tái sử dụng. Nó kết nối với các trợ lý AI và tích hợp mượt mà vào quy trình phát triển (development workflow)."
}