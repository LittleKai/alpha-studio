{
  "source": "shyft.ai",
  "url": "https://shyft.ai/skills/gemini-cli-skill",
  "slug": "gemini-cli-skill",
  "name": "browser-use",
  "headline": "Gemini CLI Skill - Code Generation & Review",
  "short_description": "Gemini CLI skill enables Claude to use Gemini 3 Pro via CLI for code generation and review. Developers benefit from automated code creation and quality checks. Integrates with existing development workflows and IDEs.",
  "tier": "Silver",
  "category": "Marketing",
  "difficulty": "Intermediate",
  "install_type": "Git Clone",
  "estimated_time_saving": "45 min",
  "author": "forayconsulting",
  "install_command": "git clone https://github.com/forayconsulting/gemini_cli_skill.git",
  "source_repo_url": "https://github.com/forayconsulting/gemini_cli_skill",
  "works_with": [
    "Claude"
  ],
  "tags": [],
  "sections": {
    "overview": "The Gemini CLI Skill teaches Claude Code to leverage Google's Gemini as an auxiliary development tool. It enables code generation, security audits, test creation, documentation, real-time web research via Google Search, and codebase architecture analysis. Developers benefit from a second AI perspective, access to current information grounding, and the ability to offload tasks in parallel while continuing their work. The skill provides ready-to-use prompt templates, integration patterns like Generate-Review-Fix workflows, and access to Gemini's unique tools including codebase_investigator for deep code analysis. Installation requires Gemini CLI and authentication; once set up, Claude Code automatically invokes the skill when appropriate for supported tasks.\nGenerate code for apps, components, and modules with Gemini\nConduct security audits and bug detection via Gemini code review\nCreate unit and integration tests automatically\nGenerate JSDoc, README, and API documentation",
    "setup": "No install command available. Check the GitHub repository for manual installation instructions.\n`git clone https://github.com/forayconsulting/gemini_cli_skill`",
    "usage": "```\nUse the gemini_cli_skill to generate and review code for [PROJECT_NAME]. Generate a [LANGUAGE] implementation for [FUNCTIONALITY_DESCRIPTION]. After generation, run a comprehensive review to identify potential bugs, performance bottlenecks, and security vulnerabilities. Provide the final code with inline comments explaining key optimizations and trade-offs. Use the following command structure: `gemini_cli_skill --generate --review --language [LANGUAGE] --output [FILE_PATH]`\n```\nCopy\n\n```\n```python\n# File: user_auth_service.py\n# Generated by gemini_cli_skill for project 'SecureApp'\n# Reviewed for security and performance issues on 2024-05-15\n\nimport bcrypt\nimport jwt\nfrom datetime import datetime, timedelta\nfrom typing import Optional\n\nclass UserAuthService:\n    def __init__(self, secret_key: str):\n        self.secret_key = secret_key\n        # Security Note: secret_key should be loaded from environment variables in production\n        \n    def hash_password(self, password: str) -> str:\n        \"\"\"Securely hash a password using bcrypt with cost factor 12\"\"\"\n        salt = bcrypt.gensalt(rounds=12)\n        return bcrypt.hashpw(password.encode('utf-8'), salt).decode('utf-8')\n        \n    def verify_password(self, password: str, hashed: str) -> bool:\n        \"\"\"Verify a password against its hashed version\"\"\"\n        return bcrypt.checkpw(password.encode('utf-8'), hashed.encode('utf-8'))\n        \n    def generate_token(self, user_id: str, expires_in: int = 3600) -> str:\n        \"\"\"Generate a JWT token with expiration\"\"\"\n        payload = {\n            'sub': user_id,\n            'iat': datetime.utcnow(),\n            'exp': datetime.utcnow() + timedelta(seconds=expires_in)\n        }\n        return jwt.encode(payload, self.secret_key, algorithm='HS256')\n        \n    def validate_token(self, token: str) -> Optional[str]:\n        \"\"\"Validate a JWT token and return user_id if valid\"\"\"\n        try:\n            payload = jwt.decode(token, self.secret_key, algorithms=['HS256'])\n            return payload['sub']\n        except jwt.ExpiredSignatureError:\n            raise ValueError(\"Token has expired\")\n        except jwt.InvalidTokenError:\n            raise ValueError(\"Invalid token\")\n\n# Performance Note: Consider using async bcrypt for high-load scenarios\n# Security Note: Implement token blacklisting for logout functionality\n```\n\n**Review Summary:**\n1. **Security:** ✅ Uses bcrypt with appropriate cost factor (12) for password hashing. JWT implementation is secure with HS256 algorithm. \n2. **Performance:** ⚠️ Synchronous bcrypt operations may cause blocking under high load. Consider async alternatives.\n3. **Bugs:** ❌ No input validation for password strength (should enforce min length/complexity)\n4. **Improvements:** Add token blacklisting mechanism for logout functionality. Consider rate limiting for auth endpoints.\n\n**Recommendations:**\n- Add password complexity requirements in hash_password()\n- Implement async bcrypt for production use\n- Add token revocation list for logout functionality\n- Consider adding refresh token mechanism\n```",
    "requirements": [
      "Claude Code or compatible AI agent",
      "Works with: Claude"
    ],
    "related_skills": [
      "gemini-cli-skill#main-content",
      "gemini-cli-skill#overview",
      "gemini-cli-skill#setup",
      "gemini-cli-skill#examples",
      "gemini-cli-skill#tools",
      "gemini-cli-skill#mcp",
      "gemini-cli-skill#related",
      "find-skills",
      "vercel-composition-patterns",
      "browser-use"
    ],
    "overview_vi": "The Gemini CLI Skill teaches Claude Code to leverage Google's Gemini as an auxiliary development tool. It enables code generation, security audits, test creation, documentation, real-time web research via Google Search, and codebase architecture analysis. Developers benefit from a second AI perspective, access to current information grounding, and the ability to offload tasks in parallel while continuing their work. The skill provides ready-to-use prompt templates, integration patterns like Generate-Review-Fix workflows, and access to Gemini's unique tools including codebase_investigator for deep code analysis. Installation requires Gemini CLI and authentication; once set up, Claude Code automatically invokes the skill when appropriate for supported tasks.\nGenerate code for apps, components, and modules with Gemini\nConduct security audits and bug detection via Gemini code review\nCreate unit and integration tests automatically\nGenerate JSDoc, README, and API documentation",
    "setup_vi": "No install command available. Check the GitHub repository for manual installation instructions.\n`git clone https://github.com/forayconsulting/gemini_cli_skill`",
    "usage_vi": "```\nUse the gemini_cli_skill to generate and review code for [PROJECT_NAME]. Generate a [LANGUAGE] implementation for [FUNCTIONALITY_DESCRIPTION]. After generation, run a comprehensive review to identify potential bugs, performance bottlenecks, and security vulnerabilities. Provide the final code with inline comments explaining key optimizations and trade-offs. Use the following command structure: `gemini_cli_skill --generate --review --language [LANGUAGE] --output [FILE_PATH]`\n```\nCopy\n\n```\n```python\n# File: user_auth_service.py\n# Generated by gemini_cli_skill for project 'SecureApp'\n# Reviewed for security and performance issues on 2024-05-15\n\nimport bcrypt\nimport jwt\nfrom datetime import datetime, timedelta\nfrom typing import Optional\n\nclass UserAuthService:\n    def __init__(self, secret_key: str):\n        self.secret_key = secret_key\n        # Security Note: secret_key should be loaded from environment variables in production\n        \n    def hash_password(self, password: str) -> str:\n        \"\"\"Securely hash a password using bcrypt with cost factor 12\"\"\"\n        salt = bcrypt.gensalt(rounds=12)\n        return bcrypt.hashpw(password.encode('utf-8'), salt).decode('utf-8')\n        \n    def verify_password(self, password: str, hashed: str) -> bool:\n        \"\"\"Verify a password against its hashed version\"\"\"\n        return bcrypt.checkpw(password.encode('utf-8'), hashed.encode('utf-8'))\n        \n    def generate_token(self, user_id: str, expires_in: int = 3600) -> str:\n        \"\"\"Generate a JWT token with expiration\"\"\"\n        payload = {\n            'sub': user_id,\n            'iat': datetime.utcnow(),\n            'exp': datetime.utcnow() + timedelta(seconds=expires_in)\n        }\n        return jwt.encode(payload, self.secret_key, algorithm='HS256')\n        \n    def validate_token(self, token: str) -> Optional[str]:\n        \"\"\"Validate a JWT token and return user_id if valid\"\"\"\n        try:\n            payload = jwt.decode(token, self.secret_key, algorithms=['HS256'])\n            return payload['sub']\n        except jwt.ExpiredSignatureError:\n            raise ValueError(\"Token has expired\")\n        except jwt.InvalidTokenError:\n            raise ValueError(\"Invalid token\")\n\n# Performance Note: Consider using async bcrypt for high-load scenarios\n# Security Note: Implement token blacklisting for logout functionality\n```\n\n**Review Summary:**\n1. **Security:** ✅ Uses bcrypt with appropriate cost factor (12) for password hashing. JWT implementation is secure with HS256 algorithm. \n2. **Performance:** ⚠️ Synchronous bcrypt operations may cause blocking under high load. Consider async alternatives.\n3. **Bugs:** ❌ No input validation for password strength (should enforce min length/complexity)\n4. **Improvements:** Add token blacklisting mechanism for logout functionality. Consider rate limiting for auth endpoints.\n\n**Recommendations:**\n- Add password complexity requirements in hash_password()\n- Implement async bcrypt for production use\n- Add token revocation list for logout functionality\n- Consider adding refresh token mechanism\n```"
  },
  "crawl_meta": {
    "crawled_at": "2026-06-30T18:25:31.282677Z",
    "http_status": 200,
    "content_hash": "d88a1772546d090afcb3942b11be7d4e8d2f8e3ef40c00ac12ec9c15e1931824",
    "raw_markdown_path": "D:\\Dev\\2.reference_pj\\.skill-ref\\crawl\\crawl4ai-skill\\output\\shyft-skills\\raw\\gemini-cli-skill.md",
    "raw_html_path": "D:\\Dev\\2.reference_pj\\.skill-ref\\crawl\\crawl4ai-skill\\output\\shyft-skills\\raw\\gemini-cli-skill.html",
    "schema_version": "1.0",
    "attempts": 1
  },
  "headline_vi": "Gemini CLI Skill - Code Generation & Review",
  "short_description_vi": "Gemini CLI skill enables Claude to use Gemini 3 Pro via CLI for code generation and review. Developers benefit from automated code creation and quality checks. Integrates with existing development workflows and IDEs."
}