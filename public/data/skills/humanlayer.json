{
  "source": "shyft.ai",
  "url": "https://shyft.ai/skills/humanlayer",
  "slug": "humanlayer",
  "name": "browser-use",
  "headline": "HumanLayer - AI Coding Agents for Complex Codebases",
  "short_description": "HumanLayer enables AI coding agents to solve complex problems in large codebases. Operations teams use it to automate code reviews, debugging, and maintenance. It integrates with IDEs and connects to AI agents like Claude.",
  "tier": "Gold",
  "category": "Marketing",
  "difficulty": "Intermediate",
  "install_type": "Git Clone",
  "estimated_time_saving": "240 min",
  "author": "humanlayer",
  "install_command": "git clone https://github.com/humanlayer/humanlayer.git",
  "source_repo_url": "https://github.com/humanlayer/humanlayer",
  "works_with": [
    "Claude"
  ],
  "tags": [
    "claude-code",
    "llm",
    "amp",
    "opencode",
    "agents",
    "llms",
    "codex",
    "ai",
    "humanlayer",
    "human-in-the-loop"
  ],
  "sections": {
    "overview": "HumanLayer is an approval workflow platform designed for AI coding agents such as Claude, enabling teams to automate code reviews, debugging, and maintenance while maintaining human oversight. It integrates with AI agents and development tools to add approval gates and human-in-the-loop controls to automated code operations. Operations and engineering teams use HumanLayer to solve complex problems in large codebases with confidence, ensuring critical code changes receive proper review before execution. The platform bridges the gap between autonomous AI capabilities and organizational security and control requirements. By connecting to AI agents and IDEs, HumanLayer enables teams to safely automate repetitive code tasks while keeping humans in control of high-impact decisions.\nAutomate code reviews by integrating AI agents to identify issues and suggest improvements.\nGenerate boilerplate code for new features based on existing project specifications.\nFacilitate parallel development by managing multiple AI coding sessions simultaneously.\nEnhance team collaboration by providing tailored workflows that integrate AI assistance into daily coding tasks.",
    "setup": "No install command available. Check the GitHub repository for manual installation instructions.\n`git clone https://github.com/humanlayer/humanlayer`",
    "usage": "```\nAct as a HumanLayer agent to solve the following issue in the [PROJECT_NAME] codebase: [ISSUE_DESCRIPTION]. Provide a step-by-step solution with code changes, tests, and validation steps. Include references to relevant files and functions. Assume you have access to the full codebase and can run tests locally.\n```\nCopy\n\n```\n### HumanLayer Agent Report: Fixing Memory Leak in `api/v1/handlers/user.go`\n\n**Issue:** The `/api/v1/users/{id}` endpoint leaks memory under high load, causing the service to crash after ~10,000 requests. Profiling shows the leak originates in the `UserHandler.GetUser()` function where the `userProfile` struct is not properly garbage collected.\n\n**Root Cause:** The `userProfile` struct contains a `map[string]interface{}` that grows unbounded when deserializing JSON responses from the database. The `sync.Pool` for `userProfile` objects is not properly reset between requests.\n\n**Solution:**\n1. **Code Changes:**\n   - Modified `api/v1/handlers/user.go` to reset the `map` field in the `userProfile` struct before returning it to the pool:\n ```go\n     func (up *userProfile) Reset() {\n         up.id = \"\"\n         up.name = \"\"\n         // Clear the map to prevent memory leaks\n         for k := range up.metadata {\n             delete(up.metadata, k)\n         }\n     }\n ```\n   - Updated the `sync.Pool` initialization in `main.go` to use the new `Reset()` method:\n ```go\n     userProfilePool = sync.Pool{\n         New: func() interface{} {\n             return &userProfile{\n                 metadata: make(map[string]interface{}),\n             }\n         },\n     }\n ```\n\n2. **Tests:**\n   - Added a new test in `api/v1/handlers/user_test.go`:\n ```go\n     func TestUserHandlerMemoryLeak(t *testing.T) {\n         // Simulate 50,000 requests\n         for i := 0; i < 50000; i++ {\n             req := httptest.NewRequest(\"GET\", \"/api/v1/users/123\", nil)\n             w := httptest.NewRecorder()\n             UserHandler.GetUser(w, req)\n             assert.Equal(t, 200, w.Code)\n         }\n         // Verify memory usage is stable\n         var m runtime.MemStats\n         runtime.ReadMemStats(&m)\n         initialHeap := m.HeapAlloc\n         time.Sleep(1 * time.Second)\n         runtime.ReadMemStats(&m)\n         assert.Less(t, m.HeapAlloc, initialHeap+1000000) // Allow 1MB growth\n     }\n ```\n\n3. **Validation:**\n   - Ran the test locally with `go test -race ./api/v1/handlers -run TestUserHandlerMemoryLeak`. All tests passed.\n   - Deployed the fix to staging and monitored memory usage with Prometheus. Memory stabilized at ~200MB (previously grew to 2GB under load).\n   - No performance regression observed in benchmarks: P99 latency improved from 45ms to 38ms.\n\n**Files Modified:**\n- `api/v1/handlers/user.go` (3 changes)\n- `api/v1/handlers/user_test.go` (1 new test)\n- `main.go` (1 change)\n\n**Next Steps:**\n- Merge PR #456 into `main` branch.\n- Deploy to production after QA sign-off.\n- Monitor memory metrics for 48 hours post-deployment.\n```",
    "requirements": [
      "Claude Code or compatible AI agent",
      "Works with: Claude"
    ],
    "related_skills": [
      "humanlayer#main-content",
      "humanlayer#overview",
      "humanlayer#setup",
      "humanlayer#examples",
      "humanlayer#tools",
      "humanlayer#mcp",
      "humanlayer#related",
      "find-skills",
      "vercel-composition-patterns",
      "browser-use"
    ],
    "overview_vi": "HumanLayer is an approval workflow platform designed for AI coding agents such as Claude, enabling teams to automate code reviews, debugging, and maintenance while maintaining human oversight. It integrates with AI agents and development tools to add approval gates and human-in-the-loop controls to automated code operations. Operations and engineering teams use HumanLayer to solve complex problems in large codebases with confidence, ensuring critical code changes receive proper review before execution. The platform bridges the gap between autonomous AI capabilities and organizational security and control requirements. By connecting to AI agents and IDEs, HumanLayer enables teams to safely automate repetitive code tasks while keeping humans in control of high-impact decisions.\nAutomate code reviews by integrating AI agents to identify issues and suggest improvements.\nGenerate boilerplate code for new features based on existing project specifications.\nFacilitate parallel development by managing multiple AI coding sessions simultaneously.\nEnhance team collaboration by providing tailored workflows that integrate AI assistance into daily coding tasks.",
    "setup_vi": "No install command available. Check the GitHub repository for manual installation instructions.\n`git clone https://github.com/humanlayer/humanlayer`",
    "usage_vi": "```\nAct as a HumanLayer agent to solve the following issue in the [PROJECT_NAME] codebase: [ISSUE_DESCRIPTION]. Provide a step-by-step solution with code changes, tests, and validation steps. Include references to relevant files and functions. Assume you have access to the full codebase and can run tests locally.\n```\nCopy\n\n```\n### HumanLayer Agent Report: Fixing Memory Leak in `api/v1/handlers/user.go`\n\n**Issue:** The `/api/v1/users/{id}` endpoint leaks memory under high load, causing the service to crash after ~10,000 requests. Profiling shows the leak originates in the `UserHandler.GetUser()` function where the `userProfile` struct is not properly garbage collected.\n\n**Root Cause:** The `userProfile` struct contains a `map[string]interface{}` that grows unbounded when deserializing JSON responses from the database. The `sync.Pool` for `userProfile` objects is not properly reset between requests.\n\n**Solution:**\n1. **Code Changes:**\n   - Modified `api/v1/handlers/user.go` to reset the `map` field in the `userProfile` struct before returning it to the pool:\n ```go\n     func (up *userProfile) Reset() {\n         up.id = \"\"\n         up.name = \"\"\n         // Clear the map to prevent memory leaks\n         for k := range up.metadata {\n             delete(up.metadata, k)\n         }\n     }\n ```\n   - Updated the `sync.Pool` initialization in `main.go` to use the new `Reset()` method:\n ```go\n     userProfilePool = sync.Pool{\n         New: func() interface{} {\n             return &userProfile{\n                 metadata: make(map[string]interface{}),\n             }\n         },\n     }\n ```\n\n2. **Tests:**\n   - Added a new test in `api/v1/handlers/user_test.go`:\n ```go\n     func TestUserHandlerMemoryLeak(t *testing.T) {\n         // Simulate 50,000 requests\n         for i := 0; i < 50000; i++ {\n             req := httptest.NewRequest(\"GET\", \"/api/v1/users/123\", nil)\n             w := httptest.NewRecorder()\n             UserHandler.GetUser(w, req)\n             assert.Equal(t, 200, w.Code)\n         }\n         // Verify memory usage is stable\n         var m runtime.MemStats\n         runtime.ReadMemStats(&m)\n         initialHeap := m.HeapAlloc\n         time.Sleep(1 * time.Second)\n         runtime.ReadMemStats(&m)\n         assert.Less(t, m.HeapAlloc, initialHeap+1000000) // Allow 1MB growth\n     }\n ```\n\n3. **Validation:**\n   - Ran the test locally with `go test -race ./api/v1/handlers -run TestUserHandlerMemoryLeak`. All tests passed.\n   - Deployed the fix to staging and monitored memory usage with Prometheus. Memory stabilized at ~200MB (previously grew to 2GB under load).\n   - No performance regression observed in benchmarks: P99 latency improved from 45ms to 38ms.\n\n**Files Modified:**\n- `api/v1/handlers/user.go` (3 changes)\n- `api/v1/handlers/user_test.go` (1 new test)\n- `main.go` (1 change)\n\n**Next Steps:**\n- Merge PR #456 into `main` branch.\n- Deploy to production after QA sign-off.\n- Monitor memory metrics for 48 hours post-deployment.\n```"
  },
  "crawl_meta": {
    "crawled_at": "2026-06-30T19:54:03.035573Z",
    "http_status": 200,
    "content_hash": "5456aa03a0427c7bf32eed420476edd2dfc813a78907f2a91944b9a21db52d85",
    "raw_markdown_path": "D:\\Dev\\2.reference_pj\\.skill-ref\\crawl\\crawl4ai-skill\\output\\shyft-skills\\raw\\humanlayer.md",
    "raw_html_path": "D:\\Dev\\2.reference_pj\\.skill-ref\\crawl\\crawl4ai-skill\\output\\shyft-skills\\raw\\humanlayer.html",
    "schema_version": "1.0",
    "attempts": 1
  },
  "headline_vi": "HumanLayer - AI Coding Agents for Complex Codebases",
  "short_description_vi": "HumanLayer enables AI coding agents to solve complex problems in large codebases. Operations teams use it to automate code reviews, debugging, and maintenance. It integrates with IDEs and connects to AI agents like Claude."
}