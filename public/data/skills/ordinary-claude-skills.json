{
  "source": "shyft.ai",
  "url": "https://shyft.ai/skills/ordinary-claude-skills",
  "slug": "ordinary-claude-skills",
  "name": "browser-use",
  "headline": "Ordinary Claude Skills - Operations Automation",
  "short_description": "A collection of Claude Skills and resources for automating operations tasks. Benefits teams using Claude by providing reusable code snippets and workflows. Connects to Claude's API for task automation.",
  "tier": "Silver",
  "category": "Marketing",
  "difficulty": "Intermediate",
  "install_type": "Git Clone",
  "estimated_time_saving": "30 min",
  "author": "Microck",
  "install_command": "git clone https://github.com/Microck/ordinary-claude-skills.git",
  "source_repo_url": "https://github.com/Microck/ordinary-claude-skills",
  "works_with": [
    "Claude"
  ],
  "tags": [
    "claude-code",
    "list",
    "collection",
    "claude",
    "claude-skills"
  ],
  "sections": {
    "overview": "Ordinary Claude Skills is a massive local repository aggregating over 600 official and community-built Claude skills organized across categories like science, software engineering, infrastructure, data, business, creative work, and web3. Skills are prompt packages and scripts that teach Claude how to perform specific tasks without requiring repeated context, loading only when needed to preserve token usage. Teams can browse the indexed static site or clone the repository locally to integrate skills into their MCP servers, custom Claude.ai implementations, or agent workflows. The standardized folder structure makes it easy to locate and configure skills for specific use cases, from API design and debugging to Kubernetes automation and smart contract development.\nDebug React components using systematic debugging and frontend design skills\nExtract competitor marketing data from landing pages automatically\nParse scanned PDFs and complex document structures into structured text\nRun comprehensive testing suites on web applications before production deployment",
    "setup": "No install command available. Check the GitHub repository for manual installation instructions.\n`git clone https://github.com/Microck/ordinary-claude-skills`",
    "usage": "```\nGenerate a reusable code snippet for [TASK] using Claude's API. The snippet should handle [SPECIFIC_FUNCTIONALITY] and include error handling for [COMMON_ERRORS]. Format the code as a Python function named [FUNCTION_NAME] with clear docstrings and type hints. Example: 'Generate a Python function called `fetch_invoices` that retrieves unpaid invoices from a database using Claude's API, handles connection errors, and returns a list of invoice IDs.'\n```\nCopy\n\n```\n```python\nimport requests\nfrom typing import List, Optional\n\ndef fetch_invoices(api_key: str, due_date: str) -> List[dict]:\n    \"\"\"\n    Retrieves a list of unpaid invoices from the database via Claude's API.\n    \n    Args:\n        api_key: Claude API key for authentication\n        due_date: ISO format date (YYYY-MM-DD) to filter overdue invoices\n    \n    Returns:\n        List of invoice dictionaries with keys: id, customer_name, amount, due_date\n        Returns empty list if no invoices found or on error\n    \n    Raises:\n        ValueError: If due_date is not in valid format\n    \"\"\"\n    if not validate_date_format(due_date):\n        raise ValueError(\"Invalid date format. Use YYYY-MM-DD\")\n    \n    headers = {\"Authorization\": f\"Bearer {api_key}\", \"Content-Type\": \"application/json\"}\n    payload = {\n        \"query\": \"SELECT id, customer_name, amount, due_date FROM invoices WHERE paid=false AND due_date < :due_date\",\n        \"params\": {\"due_date\": due_date}\n    }\n    \n    try:\n        response = requests.post(\n            \"https://api.claude.com/v1/query\",\n            json=payload,\n            headers=headers,\n            timeout=10\n        )\n        response.raise_for_status()\n        return response.json().get(\"results\", [])\n    except requests.exceptions.RequestException as e:\n        print(f\"API request failed: {str(e)}\")\n        return []\n\ndef validate_date_format(date_str: str) -> bool:\n    \"\"\"Validates ISO 8601 date format (YYYY-MM-DD)\"\"\"\n    try:\n        from datetime import datetime\n        datetime.strptime(date_str, \"%Y-%m-%d\")\n        return True\n    except ValueError:\n        return False\n```\n\n**Usage Example:**\n```python\n# In your Claude project\nauth_key = \"claude_sk_xyz123\"\noverdue_invoices = fetch_invoices(auth_key, \"2023-11-15\")\n\nif overdue_invoices:\n    print(f\"Found {len(overdue_invoices)} overdue invoices\")\n    for invoice in overdue_invoices:\n        print(f\"Invoice {invoice['id']} for ${invoice['amount']} due {invoice['due_date']}\")\nelse:\n    print(\"No overdue invoices found or API error occurred\")\n```\n```",
    "requirements": [
      "Claude Code or compatible AI agent",
      "Works with: Claude"
    ],
    "related_skills": [
      "ordinary-claude-skills#main-content",
      "ordinary-claude-skills#overview",
      "ordinary-claude-skills#setup",
      "ordinary-claude-skills#examples",
      "ordinary-claude-skills#tools",
      "ordinary-claude-skills#mcp",
      "ordinary-claude-skills#related",
      "find-skills",
      "vercel-composition-patterns",
      "browser-use"
    ],
    "overview_vi": "Ordinary Claude Skills is a massive local repository aggregating over 600 official and community-built Claude skills organized across categories like science, software engineering, infrastructure, data, business, creative work, and web3. Skills are prompt packages and scripts that teach Claude how to perform specific tasks without requiring repeated context, loading only when needed to preserve token usage. Teams can browse the indexed static site or clone the repository locally to integrate skills into their MCP servers, custom Claude.ai implementations, or agent workflows. The standardized folder structure makes it easy to locate and configure skills for specific use cases, from API design and debugging to Kubernetes automation and smart contract development.\nDebug React components using systematic debugging and frontend design skills\nExtract competitor marketing data from landing pages automatically\nParse scanned PDFs and complex document structures into structured text\nRun comprehensive testing suites on web applications before production deployment",
    "setup_vi": "No install command available. Check the GitHub repository for manual installation instructions.\n`git clone https://github.com/Microck/ordinary-claude-skills`",
    "usage_vi": "```\nGenerate a reusable code snippet for [TASK] using Claude's API. The snippet should handle [SPECIFIC_FUNCTIONALITY] and include error handling for [COMMON_ERRORS]. Format the code as a Python function named [FUNCTION_NAME] with clear docstrings and type hints. Example: 'Generate a Python function called `fetch_invoices` that retrieves unpaid invoices from a database using Claude's API, handles connection errors, and returns a list of invoice IDs.'\n```\nCopy\n\n```\n```python\nimport requests\nfrom typing import List, Optional\n\ndef fetch_invoices(api_key: str, due_date: str) -> List[dict]:\n    \"\"\"\n    Retrieves a list of unpaid invoices from the database via Claude's API.\n    \n    Args:\n        api_key: Claude API key for authentication\n        due_date: ISO format date (YYYY-MM-DD) to filter overdue invoices\n    \n    Returns:\n        List of invoice dictionaries with keys: id, customer_name, amount, due_date\n        Returns empty list if no invoices found or on error\n    \n    Raises:\n        ValueError: If due_date is not in valid format\n    \"\"\"\n    if not validate_date_format(due_date):\n        raise ValueError(\"Invalid date format. Use YYYY-MM-DD\")\n    \n    headers = {\"Authorization\": f\"Bearer {api_key}\", \"Content-Type\": \"application/json\"}\n    payload = {\n        \"query\": \"SELECT id, customer_name, amount, due_date FROM invoices WHERE paid=false AND due_date < :due_date\",\n        \"params\": {\"due_date\": due_date}\n    }\n    \n    try:\n        response = requests.post(\n            \"https://api.claude.com/v1/query\",\n            json=payload,\n            headers=headers,\n            timeout=10\n        )\n        response.raise_for_status()\n        return response.json().get(\"results\", [])\n    except requests.exceptions.RequestException as e:\n        print(f\"API request failed: {str(e)}\")\n        return []\n\ndef validate_date_format(date_str: str) -> bool:\n    \"\"\"Validates ISO 8601 date format (YYYY-MM-DD)\"\"\"\n    try:\n        from datetime import datetime\n        datetime.strptime(date_str, \"%Y-%m-%d\")\n        return True\n    except ValueError:\n        return False\n```\n\n**Usage Example:**\n```python\n# In your Claude project\nauth_key = \"claude_sk_xyz123\"\noverdue_invoices = fetch_invoices(auth_key, \"2023-11-15\")\n\nif overdue_invoices:\n    print(f\"Found {len(overdue_invoices)} overdue invoices\")\n    for invoice in overdue_invoices:\n        print(f\"Invoice {invoice['id']} for ${invoice['amount']} due {invoice['due_date']}\")\nelse:\n    print(\"No overdue invoices found or API error occurred\")\n```\n```"
  },
  "crawl_meta": {
    "crawled_at": "2026-06-30T19:34:20.473344Z",
    "http_status": 200,
    "content_hash": "d2f715b69199cc89c57ae56814a6aa2fe3b80b94b0207ca0d1d2c1a9e61947b7",
    "raw_markdown_path": "D:\\Dev\\2.reference_pj\\.skill-ref\\crawl\\crawl4ai-skill\\output\\shyft-skills\\raw\\ordinary-claude-skills.md",
    "raw_html_path": "D:\\Dev\\2.reference_pj\\.skill-ref\\crawl\\crawl4ai-skill\\output\\shyft-skills\\raw\\ordinary-claude-skills.html",
    "schema_version": "1.0",
    "attempts": 1
  },
  "headline_vi": "Ordinary Claude Skills - Operations Automation",
  "short_description_vi": "A collection of Claude Skills and resources for automating operations tasks. Benefits teams using Claude by providing reusable code snippets and workflows. Connects to Claude's API for task automation."
}